{% if params.phase %}
<div class="app-phase" role="complementary">
    {% if params.fullWidth %}
    <div class="nhsuk-width-container-fluid app-phase__container app-width-container">
    {% else %}
    <div class="nhsuk-width-container app-phase__container app-width-container">
    {% endif %}
        <p class="app-phase__content">
        <strong class="nhsuk-tag app-phase__tag">
            {{ params.phase }}
        </strong>
        <span class="app-phase__text">
        {% if params.bannerMsg %}
            {{ params.bannerMsg }}
        {% else %}
            This is a new service.
            {% if params.url %} – your <a class="nhsuk-link" href="{{ params.url }}">feedback</a> will help us to improve it. {% endif %}
        {% endif %}
        </span>
        </p>
    </div>
</div>
{% endif %}